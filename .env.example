# Docker Compose 环境变量模板, 复制为 .env 并替换为真实值
# 生成 JWT 密钥: openssl rand -hex 32
MYSQL_ROOT_PASSWORD=change-me-strong-db-password
MYSQL_DATABASE=hinay_admin
JWT_SECRET=change-me-openssl-rand-hex-32-result
